# Base URL of the FareSight FastAPI backend.
# Copy this file to .env.local and adjust as needed.
# The frontend shows an "API Offline" state when the backend is unreachable.
VITE_API_BASE_URL=http://localhost:8000
